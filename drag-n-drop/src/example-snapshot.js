// Draggable
const draggableSnapshot = {
  isDragging: true,
  draggingOver: 'column-1',  // OR null when we don't drag over any droppable
};

// Droppable
const droppableSnapshot = {
  isDraggingOver: true,
  draggingOverWith: 'task-1', // OR null same thing but different context
};